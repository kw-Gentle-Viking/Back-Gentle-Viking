# 로컬 시연용 환경설정

이 문서는 `Back-Gentle-Viking` 백엔드 서버를 다른 사람이 로컬에서 같은 방식으로 실행하기 위한 설정 안내입니다.

## 1. 필요한 프로그램

아래 프로그램을 먼저 설치합니다.

- Anaconda 또는 Miniconda
- Docker Desktop 또는 Docker Engine
- Git

설치 확인:

```bash
conda --version
docker --version
docker compose version
git --version
```

Python은 Conda 환경에서 `3.11` 버전으로 생성해서 사용합니다.

## 2. 프로젝트 받기

```bash
git clone <백엔드_레포_URL>
cd Back-Gentle-Viking
```

## 3. Conda 가상환경 만들기

Anaconda Prompt 또는 터미널에서 아래 명령을 실행합니다.

```bash
conda create -n gentle-viking-back python=3.11 -y
conda activate gentle-viking-back
```

이미 환경을 만든 적이 있으면 다음부터는 활성화만 하면 됩니다.

```bash
conda activate gentle-viking-back
```

## 4. 라이브러리 한 번에 설치

필요한 Python 라이브러리는 모두 `requirements.txt`에 들어 있습니다. 아래 명령어 하나로 `requirements.txt`에 적힌 라이브러리를 전부 설치합니다.

```bash
pip install -r requirements.txt
```

패키지 설치 중 pip 버전 경고가 나오면 아래 명령을 먼저 실행한 뒤 다시 설치합니다.

```bash
python -m pip install --upgrade pip
pip install -r requirements.txt
```

## 5. 로컬 환경 파일 만들기

레포에는 실제 비밀키가 들어간 `.env`를 올리지 않았습니다. 대신 예시 파일을 복사해서 로컬용 `.env`를 만듭니다.

```bash
cp .env.example .env
```

`.env.example`은 현재 로컬 실행에 쓰는 `.env`와 같은 변수 구성으로 맞춰져 있습니다. 복사한 뒤 본인 계정의 API 키 값을 `.env`에 채웁니다.

키 설정이 필요한 항목은 아래와 같습니다.

```dotenv
NAVER_CLIENT_ID=네이버_개발자센터_CLIENT_ID
NAVER_CLIENT_SECRET=네이버_개발자센터_CLIENT_SECRET
GEMINI_API_KEY=구글_AI_STUDIO_GEMINI_API_KEY
DART_API_KEY=금융감독원_DART_API_KEY
KIS_APP_KEY=한국투자증권_APP_KEY
KIS_APP_SECRET=한국투자증권_APP_SECRET
KIS_ACC_NO=한국투자증권_계좌번호
```

키를 넣을 때 주의할 점:

- `.env`는 `.gitignore`에 포함되어 있으므로 GitHub에 올리면 안 됩니다.
- `.env.example`에는 실제 키를 넣지 않고 변수 이름과 예시 설명만 남깁니다.
- 실제 주문을 막으려면 `KIS_REAL_TRADING_ENABLED=false`를 유지합니다.
- KIS를 모의 설정으로 실행하려면 `KIS_MOCK=true`를 유지합니다.

## 6. 로컬 DB와 캐시 실행

백엔드 실행에 필요한 Postgres, TimescaleDB, Redis, ClickHouse를 Docker Compose로 띄웁니다.

```bash
docker compose up -d app-db market-db train-db ai-stock-db redis clickhouse
```

상태 확인:

```bash
docker compose ps
```

포트는 기본적으로 아래처럼 열립니다.

- 백엔드 운영 DB: `localhost:5432`
- 시장 데이터 DB: `localhost:5433`
- 학습 데이터 DB: `localhost:5434`
- AI 주식 DB: `localhost:5435`
- Redis: `localhost:6379`
- ClickHouse HTTP: `localhost:8123`

## 7. 백엔드 서버 실행

Conda 환경이 켜진 상태에서 실행합니다.

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

실행 확인:

```bash
curl http://localhost:8000/
curl http://localhost:8000/health
```

정상 실행이면 `/` 요청에서 아래처럼 응답합니다.

```json
{"status":"ok","service":"control-plane"}
```

API 문서는 브라우저에서 확인할 수 있습니다.

```text
http://localhost:8000/docs
```

## 8. 프론트엔드와 같이 시연할 때

프론트엔드가 `localhost:3000` 또는 `localhost:3001`에서 실행되면 기본 `.env.example`의 `CORS_ORIGINS` 설정으로 접근할 수 있습니다.

프론트엔드 주소가 다르면 `.env`의 `CORS_ORIGINS`에 해당 주소를 추가합니다.

```dotenv
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
```

## 9. 종료 방법

백엔드 서버는 터미널에서 `Ctrl + C`로 종료합니다.

Docker 서비스는 아래 명령으로 종료합니다.

```bash
docker compose down
```

DB 데이터까지 전부 삭제하고 처음부터 다시 시작하려면 아래 명령을 사용합니다.

```bash
docker compose down -v
```

## 10. 자주 나는 문제

### DB 연결 실패

먼저 Docker 컨테이너가 떠 있는지 확인합니다.

```bash
docker compose ps
```

DB 컨테이너가 꺼져 있으면 다시 실행합니다.

```bash
docker compose up -d app-db market-db train-db ai-stock-db redis clickhouse
```

### 포트 충돌

이미 `5432`, `5433`, `5434`, `5435`, `6379`, `8123`, `8000` 포트를 다른 프로그램이 쓰고 있으면 실행이 실패할 수 있습니다.

백엔드 포트만 바꾸려면 예를 들어 아래처럼 실행합니다.

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
```

이 경우 프론트엔드의 백엔드 API 주소도 `http://localhost:8001`로 맞춰야 합니다.

### 외부 API 키가 없는 경우

`.env.example`은 실제 `.env`와 같은 변수 목록을 제공하되 비밀키 값은 비워 둡니다. KIS, Gemini, Naver, DART 기능까지 보여줘야 하면 각 서비스에서 발급받은 키를 `.env`에 넣어야 합니다.

### Docker Compose 전체 실행 주의

현재 로컬 시연 기준으로는 아래처럼 필요한 인프라 서비스만 실행하는 방식을 권장합니다.

```bash
docker compose up -d app-db market-db train-db ai-stock-db redis clickhouse
```

`docker compose up`만 실행하면 `api`, `backtest-worker` 서비스까지 빌드하려고 시도할 수 있습니다. 이 레포의 일반 로컬 시연 절차는 백엔드를 Conda 환경에서 직접 실행하는 방식입니다.
